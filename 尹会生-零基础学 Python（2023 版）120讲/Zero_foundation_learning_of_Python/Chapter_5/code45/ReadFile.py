# NOTE: 文件读取
#  读取文件内容，可以采用多个函数或语句实现
#  read([size])返回指定size大小的内容，如果不指定size，将返回整个文件
#  readline()返回一行数据
#  readlines([size])返回指定size行数，如果不指定size，将返回全部文件
#  for line in file迭代文件的每一行

# NOTE: 配合不同函数可以实现指定位置的文件写入：
#  tell()函数——返回文件指针位置
#  seek(偏移量,[起始位置])——移动指针
#  close()——关闭文件
